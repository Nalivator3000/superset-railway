# Настройка линейных графиков ARPPU и депозитов за первую неделю/месяц

Эта инструкция поможет создать 2 **линейных графика (Line Charts)** для анализа поведения пользователей в первую неделю и первый месяц после FTD (First Time Deposit).

**Тип визуализации:** Line Chart (линейный график) с двумя линиями на каждом графике:
- Линия 1: Количество депозитов
- Линия 2: Сумма депозитов

**Условия анализа:**
- Только пользователи, у которых есть информация начиная с FTD
- Только пользователи, от FTD которых прошел минимум 1 месяц
- Группировка по неделям/месяцам FTD

---

## Шаг 1: Создание Datasets

### 1.1. Dataset для недельного анализа

1. **Откройте Superset** → **SQL Lab**
2. **Скопируйте SQL-запрос** из файла `arppu_first_period_weekly.sql`
3. **Вставьте запрос** в SQL Lab
4. **Выполните запрос** (Run) - проверьте данные
   - Должны быть колонки: `ftd_week_start`, `users_count`, `total_deposits_week1`, `total_revenue_week1`, `arppu_week1`, `avg_deposits_week1`
5. **Сохраните как Dataset:**
   - Нажмите **"Save"** → **"Save dataset"**
   - **Table name**: `arppu_first_period_weekly`
   - Нажмите **"Save"**

### 1.2. Dataset для месячного анализа

1. **В SQL Lab** откройте новый запрос
2. **Скопируйте SQL-запрос** из файла `arppu_first_period_monthly.sql`
3. **Вставьте запрос** в SQL Lab
4. **Выполните запрос** (Run) - проверьте данные
   - Должны быть колонки: `ftd_month_start`, `users_count`, `total_deposits_month1`, `total_revenue_month1`, `arppu_month1`, `avg_deposits_month1`
5. **Сохраните как Dataset:**
   - Нажмите **"Save"** → **"Save dataset"**
   - **Table name**: `arppu_first_period_monthly`
   - Нажмите **"Save"**

---

## Шаг 2: Создание метрик в Datasets

### 2.1. Метрики для недельного Dataset

1. **Откройте Dataset** `arppu_first_period_weekly`
2. Перейдите на вкладку **"Metrics"**
3. **Создайте следующие метрики:**

**Метрика 1: Total Deposits Week 1**
- **Metric name**: `Total Deposits Week 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `SUM(total_deposits_week1)`
- Нажмите **"Save"**

**Метрика 2: Total Revenue Week 1**
- **Metric name**: `Total Revenue Week 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `SUM(total_revenue_week1)`
- Нажмите **"Save"**

**Метрика 3: ARPPU Week 1**
- **Metric name**: `ARPPU Week 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `AVG(arppu_week1)`
- Нажмите **"Save"**

**Метрика 4: Avg Deposits Week 1**
- **Metric name**: `Avg Deposits Week 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `AVG(avg_deposits_week1)`
- Нажмите **"Save"**

### 2.2. Метрики для месячного Dataset

1. **Откройте Dataset** `arppu_first_period_monthly`
2. Перейдите на вкладку **"Metrics"**
3. **Создайте следующие метрики:**

**Метрика 1: Total Deposits Month 1**
- **Metric name**: `Total Deposits Month 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `SUM(total_deposits_month1)`
- Нажмите **"Save"**

**Метрика 2: Total Revenue Month 1**
- **Metric name**: `Total Revenue Month 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `SUM(total_revenue_month1)`
- Нажмите **"Save"**

**Метрика 3: ARPPU Month 1**
- **Metric name**: `ARPPU Month 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `AVG(arppu_month1)`
- Нажмите **"Save"**

**Метрика 4: Avg Deposits Month 1**
- **Metric name**: `Avg Deposits Month 1`
- **Metric type**: `Custom SQL`
- **SQL expression**: `AVG(avg_deposits_month1)`
- Нажмите **"Save"**

---

## Шаг 3: Создание линейных графиков (Line Charts)

### 3.1. Chart 1: Недельный анализ - Линейный график (Количество депозитов + Сумма депозитов)

1. **Откройте Dataset** `arppu_first_period_weekly` → нажмите **"Explore"**
2. **Выберите Chart Type**: **Line Chart** (иконка линейного графика)

**Настройка Query:**
- **Query Mode**: `Aggregate` ⚠️ **ВАЖНО!**
- **Dimensions**: 
  - Перетащите `ftd_week_start` в поле **Dimensions** (для оси X)
- **Metrics**: 
  - Перетащите `Total Deposits Week 1` в поле **Metrics**
  - Перетащите `Total Revenue Week 1` в поле **Metrics**

**Настройка визуализации (вкладка Customize):**
- **Chart Type**: **Line Chart** (убедитесь, что выбран)
- **Time Column**: `ftd_week_start`
- **X Axis**: `ftd_week_start`
- **Y Axis**: выберите `Total Revenue Week 1` (основная метрика - левая ось)
- **Series**: оставьте пустым (метрики автоматически создадут разные линии)
- **Show Legend**: включите ✅ (для отображения легенды с названиями метрик)
- **Stack**: оставьте выключенным (для отдельных линий, не стека)

**Настройка Dual Axis (две оси Y) для разных масштабов:**

⚠️ **Проблема:** Если метрики имеют сильно разные масштабы (например, ARPPU 20-80, а Avg Deposits 2-4), одна линия будет плохо видна.

**Решение - Dual Axis:**

1. **В разделе Metrics** (вкладка Data):
   - Найдите метрику `Total Deposits Week 1` (или `Avg Deposits Week 1`)
   - Кликните на иконку настроек (⚙️) рядом с метрикой
   - Найдите опцию **"Y Axis"** или **"Axis"**
   - Выберите **"Right Y Axis"** или **"Secondary Y Axis"**
   - Если такой опции нет, попробуйте:
     - В Customize найдите раздел **"Y Axis"** или **"Right Y Axis"**
     - Включите опцию **"Use Secondary Y Axis"** или **"Dual Axis"**

2. **Альтернативный способ (если опции выше нет):**
   - В Customize найдите раздел **"Y Axis Bounds"** или **"Y Axis Format"**
   - Для каждой метрики можно настроить отдельный формат
   - Или используйте опцию **"Y Axis 2"** / **"Secondary Y Axis"**

3. **Проверка:**
   - После настройки на графике должны появиться **две оси Y**:
     - Левая ось (основная): для `Total Revenue Week 1` (большие значения)
     - Правая ось (вторичная): для `Total Deposits Week 1` (малые значения)
   - Обе линии должны быть хорошо видны и иметь разный масштаб

**Если Dual Axis недоступен:**
- Можно создать два отдельных графика
- Или использовать нормализацию данных (но это сложнее)

3. **Нажмите "Update chart"** для предпросмотра
4. **Сохраните Chart:**
   - **Chart name**: `ARPPU First Week - Deposits & Revenue`
   - **Description**: `Линейный график: количество депозитов и сумма депозитов за первую неделю по неделям FTD`

**Результат:** Две линии на графике:
- Одна линия для `Total Deposits Week 1` (количество депозитов)
- Одна линия для `Total Revenue Week 1` (сумма депозитов)

### 3.2. Chart 2: Месячный анализ - Линейный график (Количество депозитов + Сумма депозитов)

1. **Откройте Dataset** `arppu_first_period_monthly` → нажмите **"Explore"**
2. **Выберите Chart Type**: **Line Chart** (иконка линейного графика)

**Настройка Query:**
- **Query Mode**: `Aggregate` ⚠️ **ВАЖНО!**
- **Dimensions**: 
  - Перетащите `ftd_month_start` в поле **Dimensions** (для оси X)
- **Metrics**: 
  - Перетащите `Total Deposits Month 1` в поле **Metrics**
  - Перетащите `Total Revenue Month 1` в поле **Metrics**

**Настройка визуализации (вкладка Customize):**
- **Chart Type**: **Line Chart** (убедитесь, что выбран)
- **Time Column**: `ftd_month_start`
- **X Axis**: `ftd_month_start`
- **Y Axis**: выберите `Total Revenue Month 1` (основная метрика - левая ось)
- **Series**: оставьте пустым (метрики автоматически создадут разные линии)
- **Show Legend**: включите ✅ (для отображения легенды с названиями метрик)
- **Stack**: оставьте выключенным (для отдельных линий, не стека)

**Настройка Dual Axis (две оси Y) для разных масштабов:**

⚠️ **Проблема:** Если метрики имеют сильно разные масштабы, одна линия будет плохо видна.

**Решение - Dual Axis:**

1. **В разделе Metrics** (вкладка Data):
   - Найдите метрику `Total Deposits Month 1` (или `Avg Deposits Month 1`)
   - Кликните на иконку настроек (⚙️) рядом с метрикой
   - Найдите опцию **"Y Axis"** или **"Axis"**
   - Выберите **"Right Y Axis"** или **"Secondary Y Axis"**

2. **Альтернативный способ:**
   - В Customize найдите раздел **"Y Axis"** или **"Right Y Axis"**
   - Включите опцию **"Use Secondary Y Axis"** или **"Dual Axis"**

3. **Проверка:**
   - На графике должны появиться **две оси Y** (левая и правая)
   - Обе линии должны быть хорошо видны

3. **Нажмите "Update chart"** для предпросмотра
4. **Сохраните Chart:**
   - **Chart name**: `ARPPU First Month - Deposits & Revenue`
   - **Description**: `Линейный график: количество депозитов и сумма депозитов за первый месяц по месяцам FTD`

**Результат:** Две линии на графике:
- Одна линия для `Total Deposits Month 1` (количество депозитов)
- Одна линия для `Total Revenue Month 1` (сумма депозитов)

---

## Шаг 4: Альтернативный вариант - Отдельные линейные графики для каждой метрики

Если хотите видеть метрики на отдельных графиках, создайте 4 линейных графика:

### 4.1. Недельный - Количество депозитов (Line Chart)

1. **Откройте Dataset** `arppu_first_period_weekly` → **"Explore"**
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_week_start`
4. **Metrics**: `Total Deposits Week 1`
5. **Customize**:
   - **X Axis**: `ftd_week_start`
   - **Y Axis**: `Total Deposits Week 1`
6. **Chart name**: `First Week - Total Deposits`

### 4.2. Недельный - Сумма депозитов (Line Chart)

1. **Откройте Dataset** `arppu_first_period_weekly` → **"Explore"**
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_week_start`
4. **Metrics**: `Total Revenue Week 1`
5. **Customize**:
   - **X Axis**: `ftd_week_start`
   - **Y Axis**: `Total Revenue Week 1`
6. **Chart name**: `First Week - Total Revenue`

### 4.3. Месячный - Количество депозитов (Line Chart)

1. **Откройте Dataset** `arppu_first_period_monthly` → **"Explore"**
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_month_start`
4. **Metrics**: `Total Deposits Month 1`
5. **Customize**:
   - **X Axis**: `ftd_month_start`
   - **Y Axis**: `Total Deposits Month 1`
6. **Chart name**: `First Month - Total Deposits`

### 4.4. Месячный - Сумма депозитов (Line Chart)

1. **Откройте Dataset** `arppu_first_period_monthly` → **"Explore"**
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_month_start`
4. **Metrics**: `Total Revenue Month 1`
5. **Customize**:
   - **X Axis**: `ftd_month_start`
   - **Y Axis**: `Total Revenue Month 1`
6. **Chart name**: `First Month - Total Revenue`

---

## Шаг 5: Создание Dashboard

1. **Dashboards** → **"+ Dashboard"**
2. **Dashboard name**: `ARPPU First Period Analysis`
3. **Description**: `Анализ ARPPU и депозитов за первую неделю и месяц после FTD`
4. Нажмите **"Save"**

---

## Шаг 6: Добавление Charts на Dashboard

1. **Откройте Dashboard** → **"Edit Dashboard"**
2. **Добавьте Charts:**
   - `ARPPU First Week - Deposits & Revenue`
   - `ARPPU First Month - Deposits & Revenue`
   - (Или все 4 графика, если создали отдельные)
3. **Расположите Charts:**
   - Недельный график сверху
   - Месячный график снизу
   - Или рядом друг с другом
4. **Настройте размеры** Charts
5. Нажмите **"Save"**

---

## Шаг 7: Добавление фильтров (опционально)

### 7.1. Фильтр по дате для недельного графика

1. Нажмите **"+ Filter"**
2. **Filter Type**: **Time Range**
3. **Dataset**: `arppu_first_period_weekly`
4. **Column**: `ftd_week_start`
5. **Filter name**: `FTD Week Range`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте недельный Chart
7. Нажмите **"Save"**

### 7.2. Фильтр по дате для месячного графика

1. Нажмите **"+ Filter"** (создайте новый)
2. **Filter Type**: **Time Range**
3. **Dataset**: `arppu_first_period_monthly`
4. **Column**: `ftd_month_start`
5. **Filter name**: `FTD Month Range`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте месячный Chart
7. Нажмите **"Save"**

---

## Шаг 8: Настройка кэширования и отключение автообновления

### Проблема: Таймауты и медленная загрузка

Если графики долго загружаются или выдают ошибки таймаута, нужно:
1. Отключить автообновление Dashboard
2. Настроить кэширование результатов
3. Обновлять данные только вручную при необходимости

### Решение 1: Отключение автообновления Dashboard

1. **Откройте Dashboard** в режиме просмотра (НЕ редактирования)
2. Найдите кнопку **"Auto-refresh"** или **"Auto Refresh"** (обычно в правом верхнем углу)
3. **Отключите автообновление:**
   - Если видите **"Auto-refresh: ON"** → кликните и выберите **"OFF"**
   - Или найдите переключатель и выключите его
4. Теперь Dashboard не будет автоматически обновлять данные

**Для ручного обновления:**
- Нажмите кнопку **"Refresh"** или **"Reload"** (обычно иконка обновления 🔄)
- Или обновите страницу (F5)

### Решение 2: Настройка кэширования для Dataset

1. **Откройте Dataset** `arppu_first_period_weekly` (или месячный)
2. Перейдите на вкладку **"Settings"** или **"Advanced"**
3. Найдите раздел **"Cache"** или **"Caching"**
4. Настройте кэширование:
   - **Cache Timeout**: установите большое значение (например, `86400` секунд = 24 часа)
   - Или **"Cache"**: включите ✅
   - **Cache Key**: можно оставить пустым или указать уникальный ключ
5. Сохраните настройки Dataset
6. Повторите для второго Dataset

### Решение 3: Настройка кэширования для Chart

1. **Откройте Chart** для редактирования
2. Перейдите на вкладку **"Settings"** или **"Advanced"**
3. Найдите раздел **"Cache"** или **"Query Cache"**
4. Настройте:
   - **Cache Timeout**: установите большое значение (например, `86400` = 24 часа)
   - Или включите **"Use Cache"** ✅
5. Сохраните Chart
6. Повторите для всех Charts

### Решение 4: Настройка кэширования на уровне Dashboard

1. **Откройте Dashboard** в режиме редактирования
2. Найдите кнопку **"Settings"** или **"Dashboard Settings"** (обычно в меню или в правом верхнем углу)
3. Найдите раздел **"Refresh"** или **"Auto Refresh"**
4. Отключите автообновление:
   - **Auto Refresh**: выключите ❌
   - **Refresh Interval**: установите `0` или оставьте пустым
5. Сохраните Dashboard

### Решение 5: Настройка через JSON metadata (для продвинутых пользователей)

Если у вас есть доступ к JSON metadata Dashboard, можно настроить там:

1. **Откройте Dashboard** в режиме редактирования
2. Найдите раздел **"JSON metadata"** или **"Metadata"** (обычно в настройках Dashboard)
3. Найдите параметр `"refresh_frequency"` и установите его в `0`:
   ```json
   "refresh_frequency": 0
   ```
   - `0` = автообновление отключено
   - Любое число > 0 = интервал автообновления в секундах

4. **Для исключения конкретных Charts из автообновления:**
   - Найдите массив `"timed_refresh_immune_slices"` 
   - Добавьте ID ваших Charts в этот массив:
   ```json
   "timed_refresh_immune_slices": [123, 456, 789]
   ```
   - Чтобы узнать ID Chart: откройте Chart отдельно, ID будет в URL или в настройках

5. **Пример полной настройки JSON metadata:**
   ```json
   {
     "chart_configuration": {},
     "global_chart_configuration": {
       "scope": {"rootPath": ["ROOT_ID"], "excluded": []},
       "chartsInScope": []
     },
     "color_scheme": "",
     "refresh_frequency": 0,  // ← Автообновление отключено
     "expanded_slices": {},
     "label_colors": {},
     "timed_refresh_immune_slices": [],  // ← Можно добавить ID Charts здесь
     "cross_filters_enabled": true,
     "default_filters": "{}"
   }
   ```

6. Сохраните изменения в JSON metadata
7. Сохраните Dashboard

### Решение 5: Использование материализованного представления (для очень больших данных)

Если запросы все еще слишком медленные, можно создать материализованное представление:

1. Создайте материализованное представление на основе SQL запроса
2. Обновляйте его вручную по расписанию (например, раз в день)
3. Используйте это представление в Dataset вместо прямого запроса

**Пример SQL для создания материализованного представления:**

```sql
CREATE MATERIALIZED VIEW arppu_first_period_weekly_mv AS
-- Ваш SQL запрос из arppu_first_period_weekly.sql
;

-- Создайте индекс для ускорения
CREATE INDEX idx_arppu_weekly_ftd ON arppu_first_period_weekly_mv(ftd_week_start);

-- Для обновления данных (выполняйте вручную или по расписанию):
REFRESH MATERIALIZED VIEW arppu_first_period_weekly_mv;
```

### Проверка результата:

После настройки:
- ✅ Dashboard не будет автоматически обновлять данные
- ✅ Charts будут использовать кэшированные результаты
- ✅ Обновление будет происходить только вручную
- ✅ Нет таймаутов при просмотре Dashboard

### Ручное обновление данных:

Когда нужно обновить данные:
1. На Dashboard нажмите кнопку **"Refresh"** или **"Reload"**
2. Или обновите страницу (F5)
3. Или откройте Chart отдельно и нажмите **"Run Query"**

---

## Шаг 9: Настройка Dual Axis (две оси Y) для разных масштабов

### Проблема: Метрики с разными масштабами плохо видны

Если на графике одна метрика имеет большие значения (например, ARPPU: 20-80), а другая маленькие (например, Avg Deposits: 2-4), линия с маленькими значениями будет плохо видна.

### Решение: Использовать Dual Axis (две оси Y)

**Способ 1: Проверка в Customize → Y Axis (ПОПРОБУЙТЕ СНАЧАЛА)**

В некоторых версиях Superset настройка Dual Axis находится в Customize:

1. Перейдите на вкладку **Customize**
2. Найдите раздел **"Y Axis"** (он должен быть виден на скриншоте)
3. Прокрутите вниз в разделе Y Axis - может быть опция:
   - **"Right Y Axis"** или **"Secondary Y Axis"**
   - Или список метрик с возможностью выбрать ось для каждой
4. Если нашли - настройте метрики на разные оси

**Способ 2: Если в Customize тоже нет - используйте два графика (РЕКОМЕНДУЕТСЯ)**

Если Dual Axis недоступен в вашей версии Superset, создайте два отдельных графика - это самое простое и надежное решение:

**Создание двух отдельных графиков (пошагово):**

**График 1: Revenue (большие значения)**

1. Создайте новый Chart на основе Dataset `arppu_first_period_weekly`
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_week_start`
4. **Metrics**: только `Total Revenue Week 1` (удалите `Total Deposits Week 1` если он там)
5. **Customize**:
   - **X Axis**: `ftd_week_start`
   - **Y Axis**: `Total Revenue Week 1`
6. **Chart name**: `ARPPU First Week - Revenue`
7. Сохраните Chart

**График 2: Deposits (малые значения)**

1. Создайте еще один Chart на основе того же Dataset
2. **Chart Type**: **Line Chart**
3. **Dimensions**: `ftd_week_start`
4. **Metrics**: только `Total Deposits Week 1` (или `Avg Deposits Week 1`)
5. **Customize**:
   - **X Axis**: `ftd_week_start`
   - **Y Axis**: `Total Deposits Week 1`
6. **Chart name**: `ARPPU First Week - Deposits`
7. Сохраните Chart

**Размещение на Dashboard:**

1. Откройте Dashboard в режиме редактирования
2. Добавьте оба графика
3. Разместите их рядом друг с другом (горизонтально) или один под другим (вертикально)
4. Каждый график будет иметь свой оптимальный масштаб Y-оси
5. Обе метрики будут отлично видны!

**Преимущества этого подхода:**
- ✅ Работает в любой версии Superset
- ✅ Каждый график имеет оптимальный масштаб
- ✅ Легко сравнивать тренды
- ✅ Не нужно искать скрытые настройки

**Способ 3: Попробуйте другой тип графика**

Если все же хотите один график с двумя осями:

1. Измените тип графика с **Line Chart** на **Time Series Chart**
2. Time Series Chart в некоторых версиях лучше поддерживает Dual Axis
3. Или попробуйте **Mixed Chart** (комбинированный график)

### Проверка результата:

После настройки Dual Axis на графике должны быть:
- ✅ **Две оси Y:**
  - Левая ось (основная): для метрики с большими значениями
  - Правая ось (вторичная): для метрики с маленькими значениями
- ✅ **Обе линии хорошо видны** и имеют подходящий масштаб
- ✅ **Легенда показывает**, какая метрика на какой оси

### Пример настройки:

**Для недельного графика:**
- **Left Y Axis**: `Total Revenue Week 1` (или `ARPPU Week 1`) - значения 20-80
- **Right Y Axis**: `Total Deposits Week 1` (или `Avg Deposits Week 1`) - значения 2-4

**Для месячного графика:**
- **Left Y Axis**: `Total Revenue Month 1` (или `ARPPU Month 1`)
- **Right Y Axis**: `Total Deposits Month 1` (или `Avg Deposits Month 1`)

---

## Готово! ✅

Теперь у вас есть:
- ✅ График количества депозитов и суммы депозитов за первую неделю
- ✅ График количества депозитов и суммы депозитов за первый месяц
- ✅ Только пользователи с FTD минимум месяц назад
- ✅ Группировка по неделям/месяцам FTD

---

## Что показывают линейные графики:

### Недельный линейный график:
- **X-axis**: Неделя FTD (когда пользователь сделал первый депозит)
- **Y-axis**: Количество депозитов и сумма депозитов за первую неделю
- **Две линии на графике**:
  - Линия 1: `Total Deposits Week 1` - количество депозитов
  - Линия 2: `Total Revenue Week 1` - сумма депозитов
- **Инсайт**: Как меняется активность пользователей в первую неделю в зависимости от времени привлечения
- **Визуализация**: Тренды по времени, легко видеть изменения и сравнить две метрики

### Месячный линейный график:
- **X-axis**: Месяц FTD (когда пользователь сделал первый депозит)
- **Y-axis**: Количество депозитов и сумма депозитов за первый месяц
- **Две линии на графике**:
  - Линия 1: `Total Deposits Month 1` - количество депозитов
  - Линия 2: `Total Revenue Month 1` - сумма депозитов
- **Инсайт**: Как меняется активность пользователей в первый месяц в зависимости от времени привлечения
- **Визуализация**: Долгосрочные тренды, сглаженные данные по месяцам

---

## Дополнительные метрики (опционально):

Можно также добавить графики для:
- **ARPPU Week 1** - средний ARPPU за первую неделю
- **Avg Deposits Week 1** - среднее количество депозитов за первую неделю
- **ARPPU Month 1** - средний ARPPU за первый месяц
- **Avg Deposits Month 1** - среднее количество депозитов за первый месяц

Эти метрики уже рассчитаны в SQL запросах и доступны в Datasets.

---

## Решение проблем

### Проблема: Одна линия плохо видна на графике (разные масштабы)

**Симптомы:**
- На графике две линии, но одна почти плоская (например, значения 2-4)
- Другая линия имеет большие значения (например, 20-80)
- Невозможно увидеть изменения в линии с маленькими значениями

**Решение 1: Найти настройку Y Axis для метрики**

1. Перейдите на вкладку **Data** (НЕ Customize!)
2. В разделе **Metrics** найдите метрику `Avg Deposits Week 1`
3. **Кликните на название метрики** или на иконку редактирования рядом
4. В открывшемся окне найдите опцию **"Y Axis"** или **"Axis"**
5. Выберите **"Right"** или **"Right Y Axis"**
6. Сохраните и обновите график

**Решение 2: Если настройка не найдена - используйте два графика**

Если не можете найти настройку Dual Axis, создайте два отдельных графика:

1. **График 1: Revenue (большие значения)**
   - Dimensions: `ftd_week_start`
   - Metrics: только `Total Revenue Week 1` или `ARPPU Week 1`
   - Y Axis будет автоматически настроен для больших значений

2. **График 2: Deposits (малые значения)**
   - Dimensions: `ftd_week_start`
   - Metrics: только `Total Deposits Week 1` или `Avg Deposits Week 1`
   - Y Axis будет автоматически настроен для малых значений

3. Разместите оба графика на Dashboard рядом друг с другом

**Решение 3: Попробуйте другой тип графика**

1. Измените тип графика с **Line Chart** на **Time Series Chart**
2. Time Series Chart обычно лучше поддерживает Dual Axis
3. Или попробуйте **Mixed Chart** (комбинированный график)

### Проблема: Не могу найти опцию Dual Axis

**Решение:**
1. Проверьте версию Superset - Dual Axis может быть недоступен в старых версиях
2. Попробуйте использовать **Time Series Chart** вместо Line Chart
3. Или создайте два отдельных графика для каждой метрики

### Проблема: График показывает неправильные данные

**Решение:**
1. Проверьте, что используете правильные метрики:
   - `Total Deposits Week 1` / `Total Deposits Month 1` - количество
   - `Total Revenue Week 1` / `Total Revenue Month 1` - сумма
2. Убедитесь, что **Query Mode** = `Aggregate`
3. Проверьте, что в **Dimensions** добавлена правильная колонка (`ftd_week_start` или `ftd_month_start`)

### Проблема: Таймаут при загрузке графиков

**Симптомы:**
- Ошибка "Timeout error: We're having trouble loading this visualization"
- "Queries are set to timeout after 60 seconds"
- Графики не загружаются или загружаются очень долго

**Решение 1: Включить асинхронные запросы (ГЛАВНОЕ РЕШЕНИЕ - РАБОТАЕТ ТОЧНО)**

⚠️ **Важно:** Параметр `connect_args` нельзя передавать напрямую в URI строке в Superset UI. Используйте асинхронные запросы.

1. **Откройте Superset** → **Data** → **Databases**
2. Найдите вашу базу данных (например, "Ubidex Events DB")
3. **Кликните на название** для редактирования
4. Прокрутите вниз до раздела **"Query Execution Options"** (в "Advanced")
5. Включите чекбокс **"Asynchronous query execution"** ✅
   - Это позволит запросам выполняться в фоне
   - Избежит таймаута веб-сервера (60 секунд)
   - Запросы будут выполняться до таймаута базы данных
6. Нажмите **"Save"**

**Решение 2: Настроить таймаут через конфигурацию Superset**

Конфигурация уже обновлена в `superset_config.py`:

```python
SQLLAB_TIMEOUT = 600  # 10 minutes
SQLLAB_ASYNC_TIME_LIMIT_SEC = 600  # 10 minutes
SUPERSET_WEBSERVER_TIMEOUT = 600  # 10 minutes
CHART_QUERY_TIMEOUT = 600  # 10 minutes
```

**Как перезапустить Superset на Railway:**

1. **Через Railway Dashboard:**
   - Откройте [Railway.app](https://railway.app) → ваш проект
   - Найдите сервис с Superset
   - Нажмите **⋮** (три точки) → **"Restart"** или **"Redeploy"**
   - Дождитесь перезапуска (1-2 минуты)

2. **Через Git Push (если настроен автодеплой):**
   ```bash
   git add superset_config.py
   git commit -m "Update timeout settings"
   git push
   ```

3. **Через Railway CLI:**
   ```bash
   railway restart
   ```

**Решение 3: Найти поле Query Timeout (если доступно)**

В разделе **"Advanced"** найдите поле **"Query Timeout"**:
- Введите `600` (10 минут) или больше
- Нажмите **"Save"**

**⚠️ ВНИМАНИЕ:** Не пытайтесь добавить `connect_args` в SQLAlchemy URI - это не работает в Superset UI!

**Решение 2: Отключите автообновление Dashboard** (см. Шаг 8 выше)
   - Это предотвратит постоянные пересчеты
   
**Решение 3: Настройте кэширование** (см. Шаг 8 выше)
   - Dataset будет использовать кэшированные результаты
   - Charts будут загружаться быстрее

**Решение 4: Оптимизируйте SQL запросы**

Добавьте фильтры по дате в SQL запросы, чтобы уменьшить объем данных:

В `arppu_first_period_weekly.sql` добавьте в CTE `eligible_users`:
```sql
AND event_date >= '2024-01-01'  -- Минимальная дата для анализа
```

**Решение 5: Создайте материализованное представление**

См. подробную инструкцию в `TIMEOUT_FIX_GUIDE.md`

**📖 Подробная инструкция:** См. `TIMEOUT_FIX_GUIDE.md` для всех способов решения проблемы таймаута

### Проблема: График не обновляется

**Решение:**
1. Нажмите кнопку **"Update chart"** после изменения настроек
2. Проверьте, нет ли желтого предупреждения "Your chart is not up to date"

### Проблема: Не видно легенды

**Решение:**
1. Перейдите на вкладку **Customize**
2. Найдите опцию **"Show Legend"**
3. Включите её ✅

---

**Готово!** Теперь у вас есть полностью настроенные линейные графики с Dual Axis для лучшей визуализации метрик с разными масштабами.

