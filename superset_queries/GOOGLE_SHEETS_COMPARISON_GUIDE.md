# Подробная инструкция: Сводная таблица для сравнения 1 HOUR и 24 HOURS

## О структуре данных

Отчет группирует данные **по кампаниям**: для каждой кампании создается одна запись с суммой всех показателей за выбранный период. Данные разделены по типам атрибуции (1h и 24h) для сравнения эффективности.

**Что показывает отчет:**
- Одна запись на кампанию для каждого типа атрибуции (1h и 24h)
- Все показатели суммируются за весь период
- Фильтрация по дате выполняется на уровне Dashboard

## О метриках

В этой инструкции мы создадим **12 метрик**, но в первый Chart добавим только **6 основных**. 

**Зачем нужны все 12 метрик?**

1. **Гибкость анализа** - можно создавать разные Chart для разных задач:
   - Chart для анализа трат (Total Spend, Total Clicks)
   - Chart для анализа эффективности (CPC, CPM, CTR)
   - Chart для анализа конверсий (CPA PostView, CPA PostClick)

2. **Разные визуализации** - некоторые метрики лучше смотреть на разных типах Chart:
   - CPM удобнее на Bar Chart
   - CPC лучше на Line Chart для трендов
   - Все метрики вместе - в Table для детального анализа

3. **Быстрое переключение** - можно добавить все метрики в один Chart и использовать фильтры Dashboard для разных анализов

4. **Сравнение с исходными данными** - метрики `Avg CPA PV` и `Avg CPA PC` позволяют сравнить рассчитанные значения с исходными данными из Google Sheets

**В первый Chart мы добавим:**
- `Total Spend`, `Total Clicks`, `Total Views` - базовые метрики
- `CPC`, `CTR`, `CPA PostView` - ключевые метрики эффективности

**Остальные метрики можно использовать:**
- В дополнительных Chart (см. Шаг 3.7)
- Добавив их в существующий Chart
- В разных типах визуализации

---

## Шаг 1: Создание Dataset

### 1.1. Открытие SQL Lab

1. Войдите в **Superset**
2. В верхнем меню нажмите **SQL Lab**
3. В левом меню выберите **SQL Editor**

### 1.2. Вставка SQL запроса

1. Откройте файл `superset_queries/google_sheets_campaigns_comparison.sql`
2. Скопируйте весь SQL код (Ctrl+A, Ctrl+C)
3. Вставьте в редактор SQL Lab (Ctrl+V)

### 1.3. Проверка запроса

1. В правом верхнем углу выберите вашу **Database** (Ubidex Events DB)
2. Нажмите кнопку **Run** (или F5)
3. Дождитесь выполнения запроса
4. Проверьте результаты:
   - Должны быть колонки: `attribution_type`, `campaign_name`, `campaignid`, `format`, `total_spend`, `total_clicks`, `min_date`, `max_date`, и т.д.
   - В колонке `attribution_type` должны быть значения: `1_hour` и `24_hours`
   - Для каждой кампании должна быть одна запись для каждого типа атрибуции (1h и 24h)
   - Колонки `min_date` и `max_date` показывают диапазон дат в данных (для фильтрации на Dashboard)

### 1.4. Сохранение Dataset

1. Если запрос выполнился успешно, нажмите кнопку **Save** (в правом верхнем углу)
2. Выберите **Save dataset**
3. В появившемся окне:
   - **Dataset name**: `Google Sheets Campaigns - Comparison`
   - **Database**: выберите вашу БД
   - **Schema**: `public` (или оставьте пустым)
   - Нажмите **Save**

## Шаг 2: Настройка Dataset (создание метрик)

### 2.1. Открытие Dataset для редактирования

1. В верхнем меню нажмите **Datasets**
2. Найдите `Google Sheets Campaigns - Comparison`
3. Нажмите на название или иконку редактирования (карандаш)

### 2.2. Создание метрик

**Важно:** В Superset метрики создаются через три поля:
- **Metric Key** - уникальный идентификатор (латинские буквы, без пробелов, подчеркивания разрешены)
- **Label** - отображаемое название (может быть на русском)
- **SQL expression** - SQL выражение для вычисления метрики

**⚠️ КРИТИЧЕСКИ ВАЖНО для SQL expression:**
- Вставляйте **ТОЛЬКО** SQL выражение, **БЕЗ** `AS`, **БЕЗ** запятых, **БЕЗ** других конструкций
- **НЕ копируйте** примеры с блоками кода (```sql ... ```) - копируйте только содержимое внутри
- Пример правильного SQL expression: `SUM(total_spend)` (только это, ничего больше!)
- Пример неправильного: `SUM(total_spend) AS total_spend` ❌ (не добавляйте AS!)

**📋 Быстрая справка:** См. файл `GOOGLE_SHEETS_METRICS_QUICK_REFERENCE.md` для готовых SQL выражений, которые можно скопировать.

**Примечание:** Если Superset автоматически создал метрики (например, `count`), вы можете:
- Оставить их, если они нужны
- Удалить их, нажав на иконку корзины (🗑️) справа от метрики

#### Метрика 1: Total Spend
1. В разделе **Metrics** нажмите **+ Add item** (синяя кнопка справа)
2. Заполните поля:
   - **Metric Key**: `total_spend` (латинскими буквами, без пробелов)
   - **Label**: `Total Spend` (отображаемое название)
   - **SQL expression**: вставьте **ТОЛЬКО** это (без кавычек, без AS):
     ```
     SUM(total_spend)
     ```
     ⚠️ **Важно:** Копируйте только `SUM(total_spend)`, ничего больше!
3. Нажмите на пустое место или другую метрику для сохранения

#### Метрика 2: Total Clicks
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `total_clicks`
   - **Label**: `Total Clicks`
   - **SQL expression**:
     ```sql
     SUM(total_clicks)
     ```

#### Метрика 3: Total Views
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `total_views`
   - **Label**: `Total Views`
   - **SQL expression**:
     ```sql
     SUM(total_views)
     ```

#### Метрика 4: Total PostView
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `total_postview`
   - **Label**: `Total PostView`
   - **SQL expression**:
     ```sql
     SUM(total_postview)
     ```

#### Метрика 5: Total PostClick
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `total_postclick`
   - **Label**: `Total PostClick`
   - **SQL expression**:
     ```sql
     SUM(total_postclick)
     ```

#### Метрика 6: CPC (Cost Per Click)
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `cpc`
   - **Label**: `CPC`
   - **SQL expression**: вставьте **ТОЛЬКО** это (без кавычек, без AS):
     ```
     SUM(total_spend) / NULLIF(SUM(total_clicks), 0)
     ```
     ⚠️ **Важно:** Копируйте только выражение, ничего больше! Не добавляйте `AS cpc` или другие конструкции.

#### Метрика 7: CPM (Cost Per Mille)
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `cpm`
   - **Label**: `CPM`
   - **SQL expression**:
     ```sql
     SUM(total_spend) / NULLIF(SUM(total_views), 0) * 1000
     ```

#### Метрика 8: CTR (Click Through Rate)
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `ctr`
   - **Label**: `CTR`
   - **SQL expression**:
     ```sql
     SUM(total_views) / NULLIF(SUM(total_clicks), 0)
     ```

#### Метрика 9: CPA PostView
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `cpa_postview`
   - **Label**: `CPA PostView`
   - **SQL expression**:
     ```sql
     SUM(total_spend) / NULLIF(SUM(total_postview), 0)
     ```

#### Метрика 10: CPA PostClick
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `cpa_postclick`
   - **Label**: `CPA PostClick`
   - **SQL expression**:
     ```sql
     SUM(total_spend) / NULLIF(SUM(total_postclick), 0)
     ```

#### Метрика 11: Avg CPA PV (из исходных данных)
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `avg_cpa_pv`
   - **Label**: `Avg CPA PV`
   - **SQL expression**:
     ```sql
     AVG(avg_cpa_pv)
     ```

#### Метрика 12: Avg CPA PC (из исходных данных)
1. Нажмите **+ Add item**
2. Заполните:
   - **Metric Key**: `avg_cpa_pc`
   - **Label**: `Avg CPA PC`
   - **SQL expression**:
     ```sql
     AVG(avg_cpa_pc)
     ```

### 2.4. Сохранение изменений

1. После создания всех метрик нажмите **Save** (в правом верхнем углу)
2. Убедитесь, что все метрики отображаются в списке

## Шаг 3: Создание Chart

### 3.1. Открытие Chart Builder

1. В Dataset нажмите кнопку **Explore** (или перейдите в **Charts** → **+ Chart**)
2. Выберите Dataset: `Google Sheets Campaigns - Comparison`

### 3.2. Настройка базовых параметров

1. **Visualization Type**: выберите **Table** (иконка таблицы вверху справа)
2. **Query mode**: выберите **Aggregate** (кнопка в разделе Query) - **ВАЖНО!**

### 3.3. Настройка Dimensions (измерения для группировки)

**Важно:** В Superset "Group by" называется **"Dimensions"**. Это поля, по которым группируются данные.

В разделе **Dimensions** добавьте следующие измерения:

**Способ 1: Перетаскивание (Drag & Drop)**
1. В левой панели найдите раздел **Columns**
2. Найдите колонку `event_date` (с иконкой часов 🕐)
3. Перетащите её в поле **Dimensions** справа (где написано "+ Drop columns here or click")
4. Найдите колонку `attribution_type` (с иконкой "abc")
5. Перетащите её в поле **Dimensions** - **ОБЯЗАТЕЛЬНО для сравнения 1h vs 24h!**
6. Найдите колонку `format` (с иконкой "abc")
7. Перетащите её в поле **Dimensions**
8. (Опционально) Перетащите `campaign_name` для детализации по кампаниям

**Способ 2: Клик по полю**
1. Кликните на поле **Dimensions** (где "+ Drop columns here or click")
2. В появившемся списке выберите `event_date`
3. Кликните на поле **Dimensions** еще раз
4. Выберите `attribution_type` ⭐ (обязательно!)
5. Кликните еще раз и выберите `format`
6. (Опционально) Добавьте `campaign_name`

**Итоговый список Dimensions:**
- `event_date` (дата события)
- `attribution_type` ⭐ (1_hour / 24_hours - обязательно для сравнения!)
- `format` (формат рекламы: banner, push, native и т.д.)
- `campaign_name` (опционально, для детализации)

### 3.4. Настройка Metrics (метрики)

В разделе **Metrics** добавьте метрики:

**Способ 1: Перетаскивание**
1. В левой панели найдите раздел **Metrics**
2. Найдите метрику `Total Spend` (с иконкой f(x))
3. Перетащите её в поле **Metrics** справа (где "+ Drop columns/metrics here or click")
4. Перетащите `Total Clicks`
5. Перетащите `Total Views`
6. Перетащите `CPC`
7. Перетащите `CTR`
8. Перетащите `CPA PostView`

**Способ 2: Клик по полю**
1. Кликните на поле **Metrics** (где "+ Drop columns/metrics here or click")
2. В появившемся списке выберите `Total Spend`
3. Кликните на поле **Metrics** еще раз
4. Выберите `Total Clicks`
5. Продолжайте добавлять остальные метрики

**Итоговый список Metrics для первого Chart:**
- `Total Spend`
- `Total Clicks`
- `Total Views`
- `CPC`
- `CTR`
- `CPA PostView`

**Примечание:** Мы создали 12 метрик, но в первый Chart добавили только 6. Остальные метрики (`Total PostView`, `Total PostClick`, `CPM`, `CPA PostClick`, `Avg CPA PV`, `Avg CPA PC`) можно использовать:
- В других Chart (создайте дополнительные Chart для разных анализов)
- В этом же Chart (просто добавьте их в поле Metrics)
- Для разных типов визуализации (например, CPM лучше смотреть на Bar Chart, а CPC на Line Chart)

### 3.5. Предпросмотр данных

1. После добавления **Dimensions** и **Metrics** нажмите кнопку **Create chart** (внизу справа) или **Run** (если кнопка называется так)
2. Дождитесь выполнения запроса
3. Проверьте результаты:
   - Данные должны отображаться в таблице
   - В колонке `attribution_type` должны быть значения `1_hour` и `24_hours`
   - Данные должны быть сгруппированы по дате, типу атрибуции и формату
   - Метрики должны отображаться как колонки

### 3.6. Сохранение Chart

1. Если данные отображаются корректно, нажмите **Save** (в правом верхнем углу)
2. В появившемся окне:
   - **Chart name**: `Campaigns Comparison 1h vs 24h`
   - **Description** (опционально): `Сравнение эффективности кампаний по типам атрибуции`
   - **Add to new dashboard**: оставьте галочку и введите название: `Google Sheets Campaigns`
   - Или снимите галочку, если Dashboard уже создан
   - Нажмите **Save**

## Шаг 3.7: Создание дополнительных Chart с остальными метриками

**Зачем нужны все 12 метрик?**

Мы создали 12 метрик, но в первый Chart добавили только 6. Остальные метрики можно использовать для:

1. **Разных типов анализа:**
   - `CPM` - для анализа стоимости на 1000 показов
   - `Total PostView` / `Total PostClick` - для анализа конверсий
   - `CPA PostClick` - для анализа эффективности PostClick атрибуции
   - `Avg CPA PV` / `Avg CPA PC` - для сравнения с исходными данными

2. **Разных визуализаций:**
   - CPM лучше смотреть на Bar Chart
   - CPC лучше на Line Chart для трендов
   - CPA метрики удобны в Table для детального анализа

3. **Гибкости в анализе:**
   - Можно быстро переключаться между метриками в одном Chart
   - Можно создать несколько Chart для разных задач

### Пример: Chart для анализа CPM

1. **Charts** → **+ Chart**
2. Выберите Dataset: `Google Sheets Campaigns - Comparison`
3. **Type**: Bar Chart
4. **Dimensions**: `format`, `attribution_type`
5. **Metrics**: `CPM` (вместо Total Spend)
6. **Save** → название: `CPM Comparison by Format`

### Пример: Chart для анализа конверсий

1. **Charts** → **+ Chart**
2. Выберите Dataset: `Google Sheets Campaigns - Comparison`
3. **Type**: Table
4. **Dimensions**: `campaign_name`, `attribution_type`
5. **Metrics**: 
   - `Total PostView`
   - `Total PostClick`
   - `CPA PostView`
   - `CPA PostClick`
6. **Save** → название: `Conversions Analysis`

### Как добавить метрики в существующий Chart

1. Откройте Chart для редактирования
2. В разделе **Metrics** добавьте нужные метрики (перетащите или выберите из списка)
3. Нажмите **Run** для обновления
4. **Save** изменения

**Совет:** Можно создать один Chart с ВСЕМИ метриками в Table формате, а затем использовать фильтры Dashboard для анализа разных аспектов.

## Шаг 4: Создание Dashboard (если еще не создан)

### 4.1. Создание Dashboard

1. В верхнем меню нажмите **Dashboards**
2. Нажмите **+ Dashboard**
3. Введите название: `Google Sheets Campaigns`
4. Нажмите **Save**

### 4.2. Добавление Chart на Dashboard

1. Если Chart уже создан, откройте Dashboard
2. Нажмите **Edit Dashboard**
3. В правом меню найдите ваш Chart `Campaigns Comparison 1h vs 24h`
4. Перетащите Chart на Dashboard
5. Растяните Chart на нужный размер (потяните за углы)

## Шаг 5: Добавление фильтров на Dashboard

### ⚠️ ВАЖНО: Как добавлять несколько фильтров

**Правило:** Для создания КАЖДОГО нового фильтра:
1. Нажимайте кнопку **Add Filter** (внизу слева в левой панели)
2. **НЕ кликайте** на существующие фильтры в левой панели - это откроет их для редактирования, а не создаст новый

**Разница:**
- ✅ **Add Filter** (кнопка внизу) = создание НОВОГО фильтра
- ❌ **Клик на фильтр** в левой панели = редактирование существующего (заменит настройки в центральной области)

### 5.1. Открытие окна настройки фильтров

1. В режиме редактирования Dashboard нажмите **+ Filter** (или иконку фильтра)
2. Откроется модальное окно **"Add and edit filters"** с тремя областями:
   - **Левая панель**: список созданных фильтров (сначала пустая)
   - **Центральная область**: настройки выбранного фильтра (вкладки Settings и Scoping)
   - **Нижняя панель**: кнопки Add Filter, Add Divider, Cancel, Save
3. В левой панели отображается список фильтров (пока пустой)
4. Нажмите кнопку **Add Filter** (внизу слева в левой панели) для создания первого фильтра

### 5.2. Фильтр 1: Диапазон дат

1. После нажатия **Add Filter** в центральной области откроются настройки нового фильтра
2. **Вкладка Settings:**
   - **Filter name** (обязательное поле, справа вверху): введите `Date Range`
   - **Filter Type** (обязательное поле, с красной звездочкой): 
     - Кликните на выпадающий список
     - Выберите **Time range** (НЕ Numerical range!)
     - ⚠️ **Важно:** Для дат используйте именно **Time range**, а не Numerical range
   - **Dataset** (обязательное поле, с красной звездочкой): 
     - Выберите `Google Sheets Campaigns - Comparison` (или `Kadam - Comparison`)
   - **Column** (обязательное поле, с красной звездочкой): 
     - Выберите `event_date` из выпадающего списка
   - **Filter Configuration** (раздел можно развернуть):
     - **Pre-filter available values**: можно оставить выключенным
     - **Single Value**: можно оставить выключенным
   - **Filter Settings** (раздел можно развернуть):
     - **Description**: можно оставить пустым или добавить описание
     - **Filter has default value**: можно включить и выбрать период (например, "Last 30 days")
     - **Filter value is required**: оставьте выключенным
3. **Вкладка Scoping:**
   - Выберите **Apply to specific charts**
   - Отметьте галочкой ваш Chart `Campaigns Comparison 1h vs 24h`
4. Нажмите **Save** (внизу справа)
5. ⚠️ **ВАЖНО:** После сохранения первого фильтра:
   - В левой панели появится фильтр "Date Range"
   - **НЕ кликайте** на него (это откроет его для редактирования)
   - Для создания второго фильтра снова нажмите кнопку **Add Filter** (внизу слева в левой панели)

### 5.3. Фильтр 2: Формат рекламы

⚠️ **ВАЖНО:** Для создания ВТОРОГО фильтра:
- **НЕ кликайте** на фильтр "Date Range" в левой панели (это откроет его для редактирования)
- **Нажмите кнопку "Add Filter"** внизу слева в левой панели (та же кнопка, что и для первого фильтра)
- В центральной области откроются пустые настройки для НОВОГО фильтра

1. В левой панели нажмите **Add Filter** еще раз (внизу слева)
2. **Вкладка Settings:**
   - **Filter name**: `Format`
   - **Filter Type**: выберите **Select** (или **Select (Multiple)** для множественного выбора)
   - **Dataset**: `Google Sheets Campaigns - Comparison`
   - **Column**: выберите `format`
   - **Filter Settings:**
     - **Filter has default value**: можно включить и выбрать "All" или конкретный формат
3. **Вкладка Scoping:**
   - **Apply to specific charts** → отметьте ваш Chart
4. Нажмите **Save**

### 5.4. Фильтр 3: Название кампании

⚠️ **ВАЖНО:** Для создания ТРЕТЬЕГО фильтра:
- **НЕ кликайте** на существующие фильтры в левой панели
- **Нажмите кнопку "Add Filter"** внизу слева

1. Нажмите **Add Filter** (внизу слева в левой панели)
2. **Вкладка Settings:**
   - **Filter name**: `Campaign`
   - **Filter Type**: выберите **Select** (или **Select (Multiple)**)
   - **Dataset**: `Google Sheets Campaigns - Comparison`
   - **Column**: выберите `campaign_name`
   - **Filter Settings:**
     - **Filter has default value**: можно включить и выбрать "All"
3. **Вкладка Scoping:**
   - **Apply to specific charts** → отметьте ваш Chart
4. Нажмите **Save**

### 5.5. Фильтр 4: Тип атрибуции (опционально)

1. Нажмите **Add Filter**
2. **Вкладка Settings:**
   - **Filter name**: `Attribution Type`
   - **Filter Type**: выберите **Select**
   - **Dataset**: `Google Sheets Campaigns - Comparison`
   - **Column**: выберите `attribution_type`
   - **Filter Settings:**
     - **Filter has default value**: можно включить и выбрать "All" или конкретный тип
3. **Вкладка Scoping:**
   - **Apply to specific charts** → отметьте ваш Chart
4. Нажмите **Save**

### 5.6. Фильтр 5: Тип события (опционально)

1. Нажмите **Add Filter**
2. **Вкладка Settings:**
   - **Filter name**: `Event Type`
   - **Filter Type**: выберите **Select**
   - **Dataset**: `Google Sheets Campaigns - Comparison`
   - **Column**: выберите `event_type`
   - **Filter Settings:**
     - **Filter has default value**: можно включить и выбрать "All"
3. **Вкладка Scoping:**
   - **Apply to specific charts** → отметьте ваш Chart
4. Нажмите **Save**

### 5.7. Фильтр 6: Минимальный Spend (опционально)

1. Нажмите **Add Filter** (внизу слева в левой панели)
2. **Вкладка Settings:**
   - **Filter name** (обязательное поле, справа вверху): введите `Min Spend`
   - **Filter Type** (обязательное поле, с красной звездочкой): 
     - Кликните на поле "Filter Type"
     - В выпадающем списке выберите **Numerical range** (НЕ "Value"!)
     - ⚠️ **Важно:** Для числового диапазона используйте именно **Numerical range**
   - **Dataset** (обязательное поле): 
     - Выберите `Google Sheets Campaigns - Comparison` (или `Kadam - Comparison`)
   - **Column** (обязательное поле): 
     - Выберите `total_spend` из выпадающего списка
   - **Filter Options** (опционально):
     - **values are dependent on other filters**: оставьте выключенным
     - **Pre-filter available values**: оставьте выключенным
     - **Sort filter values**: можно включить для сортировки значений
   - **Filter Settings** (раздел можно развернуть):
     - **Description**: можно оставить пустым или добавить описание (например, "Минимальный Spend для фильтрации кампаний")
     - **Filter has default value**: 
       - ⚠️ **Важно:** Если включите эту опцию, нужно указать конкретное значение
       - **Вариант A (рекомендуется):** Оставьте выключенным - пользователь сам выберет значение
       - **Вариант B:** Включите и в поле **Default Value** введите число (например, `0` или `1000`)
         - ⚠️ **НЕ выбирайте** из списка "1000 options" - это вызовет ошибку!
         - Введите число вручную в поле Default Value
     - **Filter value is required**: оставьте выключенным
3. **Вкладка Scoping:**
   - Выберите **Apply to specific charts**
   - Отметьте галочкой ваш Chart `Campaigns Comparison 1h vs 24h`
4. Нажмите **Save** (внизу справа)

**Как работает фильтр Numerical range:**
- На Dashboard появится поле с двумя значениями: минимальное и максимальное
- Пользователь может указать диапазон (например, от 1000 до 10000)
- Или указать только минимальное значение (оставить максимальное пустым)
- Chart будет показывать только записи, где `total_spend` попадает в указанный диапазон

### 5.8. Проверка созданных фильтров

После создания всех фильтров в левой панели должно отображаться несколько фильтров:
- Date Range
- Format
- Campaign
- (и другие, если вы их создали)

**Если видите только один фильтр** - значит вы редактировали существующий вместо создания нового. В этом случае:
1. Удалите неправильный фильтр (иконка корзины)
2. Создайте фильтры заново, каждый раз нажимая **Add Filter**

### 5.9. Редактирование существующих фильтров

1. В модальном окне **"Add and edit filters"** в левой панели **кликните** на нужный фильтр
2. В центральной области откроются его настройки
3. Измените нужные параметры во вкладке **Settings** или **Scoping**
4. Нажмите **Save**

### 5.10. Удаление фильтров

1. В модальном окне **"Add and edit filters"** в левой панели нажмите на иконку корзины (🗑️) справа от фильтра
2. Фильтр будет удален

### 5.11. Закрытие окна фильтров

1. После создания всех нужных фильтров нажмите **Save** (внизу справа) или закройте окно крестиком (X)
2. Фильтры появятся на Dashboard
3. Вы можете перетащить их в удобное место на Dashboard (в режиме редактирования)

### 5.12. Настройка отображения фильтров на Dashboard

1. В режиме редактирования Dashboard фильтры отображаются как элементы
2. Вы можете:
   - Перетащить фильтры в нужное место (обычно сверху Dashboard)
   - Изменить размер фильтров (потяните за углы)
   - Упорядочить фильтры (перетащите их)
3. Сохраните Dashboard

### 5.13. Использование фильтров

1. Выйдите из режима редактирования Dashboard
2. Фильтры станут активными
3. Измените значения фильтров:
   - **Date Range**: выберите диапазон дат
   - **Format**: выберите формат рекламы
   - **Campaign**: выберите кампанию
   - И т.д.
4. Chart автоматически обновится с учетом выбранных фильтров

## Шаг 6: Настройка визуализации Chart

### 6.1. Изменение типа визуализации

1. Откройте Chart для редактирования (нажмите на Chart на Dashboard → **Edit chart**)
2. В разделе **Visualization Type** выберите нужный тип:

#### Вариант A: Table (таблица)
- Показывает все данные в табличном виде
- Удобно для детального анализа
- Можно сортировать по колонкам

#### Вариант B: Bar Chart (столбчатая диаграмма)
1. Выберите тип **Bar Chart** (иконка столбчатой диаграммы)
2. В разделе **Dimensions** добавьте: `format`, `attribution_type`
3. В разделе **Metrics** добавьте: `Total Spend`
4. Нажмите **Create chart** или **Run**
5. Перейдите на вкладку **Customize** (вверху справа):
   - **X-axis**: выберите `attribution_type` (для сравнения 1h vs 24h)
   - **Y-axis**: выберите `Total Spend` (или другая метрика)
   - **Series**: выберите `format` (для группировки по форматам)
- Удобно для визуального сравнения

#### Вариант C: Bar Chart (столбчатая диаграмма) - сравнение по кампаниям
1. Выберите тип **Bar Chart** (иконка столбчатой диаграммы)
2. В разделе **Dimensions** добавьте: `campaign_name`, `attribution_type`
3. В разделе **Metrics** добавьте: `Total Spend` (или другие метрики)
4. Нажмите **Create chart** или **Run**
5. Перейдите на вкладку **Customize**:
   - **X-axis**: выберите `campaign_name` (для группировки по кампаниям)
   - **Y-axis**: выберите `Total Spend` (или другая метрика)
   - **Series**: выберите `attribution_type` (для сравнения 1h и 24h)
- Удобно для сравнения эффективности кампаний

#### Вариант D: Pivot Table (сводная таблица)
1. Выберите тип **Pivot Table** (если доступен)
2. В разделе **Dimensions** добавьте: `format`, `campaign_name`, `attribution_type`
3. В разделе **Metrics** добавьте: `Total Spend`, `CPC`, `CTR`
4. Перейдите на вкладку **Customize**:
   - **Rows**: выберите `format`, `campaign_name`
   - **Columns**: выберите `attribution_type`
   - **Values**: выберите `Total Spend`, `CPC`, `CTR`
- Удобно для детального сравнения

### 6.2. Настройка отображения

1. В разделе **Customize** настройте:
   - **Show totals**: включите для показа итогов
   - **Page length**: установите количество строк (например, 50)
   - **Show search**: включите для поиска по таблице

### 6.3. Сохранение изменений

1. Нажмите **Run** для предпросмотра
2. Если все устраивает, нажмите **Save**
3. Изменения автоматически применятся на Dashboard

## Шаг 7: Финальная настройка Dashboard

### 7.1. Расположение элементов

1. В режиме редактирования Dashboard:
   - Перетащите фильтры в удобное место (обычно сверху)
   - Расположите Chart ниже фильтров
   - Растяните Chart на нужный размер

### 7.2. Сохранение Dashboard

1. Нажмите **Save** (в правом верхнем углу)
2. Выйдите из режима редактирования (кнопка **Exit** или **View**)

### 7.3. Проверка работы

1. Попробуйте изменить фильтры:
   - Выберите другой диапазон дат
   - Выберите конкретный формат
   - Выберите конкретную кампанию
2. Проверьте, что Chart обновляется при изменении фильтров
3. Проверьте, что данные для `1_hour` и `24_hours` отображаются отдельно

## Примеры использования

### Пример 1: Сравнение трат по форматам

**Настройки Chart:**
- **Type**: Bar Chart (выберите иконку столбчатой диаграммы)
- **Dimensions**: перетащите `format` и `attribution_type` в поле Dimensions
- **Metrics**: перетащите `Total Spend` в поле Metrics
- После создания Chart перейдите на вкладку **Customize**:
  - **X-axis**: выберите `format`
  - **Series**: выберите `attribution_type`
  - **Y-axis**: выберите `Total Spend`

**Результат**: Два столбца для каждого формата (один для 1h, другой для 24h)

### Пример 2: Сравнение CPC по кампаниям

**Настройки Chart:**
- **Type**: Bar Chart (выберите иконку столбчатой диаграммы)
- **Dimensions**: перетащите `campaign_name` и `attribution_type` в поле Dimensions
- **Metrics**: перетащите `CPC` в поле Metrics
- После создания Chart перейдите на вкладку **Customize**:
  - **X-axis**: выберите `campaign_name`
  - **Series**: выберите `attribution_type`
  - **Y-axis**: выберите `CPC`

**Результат**: Два столбца для каждой кампании (один для 1h, другой для 24h) - сравнение CPC по кампаниям

### Пример 3: Детальная таблица по кампаниям

**Настройки Chart:**
- **Type**: Table (выберите иконку таблицы)
- **Dimensions**: перетащите `campaign_name` и `attribution_type` в поле Dimensions
- **Metrics**: перетащите `Total Spend`, `Total Clicks`, `CPC`, `CTR`, `CPA PostView` в поле Metrics
- **Sort query by**: перетащите `Total Spend` в поле "Sort query by" и выберите DESC

**Результат**: Таблица с данными по каждой кампании, разделенная по типам атрибуции

### Пример 4: Анализ CPM (Cost Per Mille)

**Настройки Chart:**
- **Type**: Bar Chart
- **Dimensions**: `format`, `attribution_type`
- **Metrics**: `CPM`
- **Customize**:
  - **X-axis**: `format`
  - **Series**: `attribution_type`
  - **Y-axis**: `CPM`

**Результат**: Сравнение стоимости на 1000 показов по форматам и типам атрибуции

### Пример 5: Анализ конверсий (PostView и PostClick)

**Настройки Chart:**
- **Type**: Table
- **Dimensions**: `campaign_name`, `attribution_type`
- **Metrics**: `Total PostView`, `Total PostClick`, `CPA PostView`, `CPA PostClick`
- **Sort query by**: `Total PostView` DESC

**Результат**: Детальный анализ конверсий по кампаниям с разделением по типам атрибуции

### Пример 6: Сравнение всех метрик в одной таблице

**Настройки Chart:**
- **Type**: Table
- **Dimensions**: `format`, `attribution_type`
- **Metrics**: ВСЕ 12 метрик:
  - `Total Spend`
  - `Total Clicks`
  - `Total Views`
  - `Total PostView`
  - `Total PostClick`
  - `CPC`
  - `CPM`
  - `CTR`
  - `CPA PostView`
  - `CPA PostClick`
  - `Avg CPA PV`
  - `Avg CPA PC`

**Результат**: Полная сводная таблица со всеми метриками для комплексного анализа

**Совет:** Такой Chart удобно использовать с фильтрами Dashboard для быстрого переключения между разными периодами, форматами и кампаниями.

## Решение проблем

### Проблема: Chart не обновляется при изменении фильтров

**Решение**: 
1. Проверьте **Scoping** для каждого фильтра
2. Убедитесь, что Chart отмечен в списке "Apply to specific charts"

### Проблема: Данные для 1h и 24h смешиваются

**Решение**:
1. Убедитесь, что `attribution_type` добавлен в **Dimensions** (перетащите колонку `attribution_type` в поле Dimensions)
2. Проверьте, что в SQL запросе есть фильтр `attribution_type IN ('1_hour', '24_hours')`

### Проблема: Метрики не отображаются

**Решение**:
1. Проверьте, что метрики созданы в Dataset
2. Убедитесь, что используете **Query Mode: Aggregate**
3. Проверьте, что метрики добавлены в раздел **Metrics** Chart

### Проблема: Ошибка "Default value is required" для фильтра Min Spend

**Ошибка:**
- В настройках фильтра включена опция **Filter has default value**
- В поле **Default Value** отображается "1000 options" или пусто
- Появляется красная ошибка: "Default value is required"

**Причина:** 
- Для фильтра типа **Numerical range** нельзя выбирать значение из списка опций
- Нужно ввести конкретное число вручную

**Решение**:
1. Откройте фильтр для редактирования (кликните на него в левой панели)
2. В разделе **Filter Settings**:
   - **Вариант A (рекомендуется):** Отключите **Filter has default value** - пользователь сам выберет значение на Dashboard
   - **Вариант B:** Если нужен дефолтный минимум:
     - Оставьте **Filter has default value** включенным
     - В поле **Default Value** введите число вручную (например, `0` или `1000`)
     - ⚠️ **НЕ выбирайте** из выпадающего списка "1000 options"!
     - Просто введите число в текстовое поле
3. Нажмите **Save**

**Правильные настройки для Min Spend:**
- **Filter Type**: **Numerical range** (НЕ "Value"!)
- **Column**: `total_spend`
- **Filter has default value**: выключено (или включено с числом, введенным вручную)

### Проблема: Ошибка "Unable to parse SQL: Error parsing near '('"

**Ошибка:**
```
Unable to parse SQL: Error parsing near '(' at line 2:5
     SUM(total_spend) AS total_spend, sql
    ^
```

**Причина:** В поле **SQL expression** добавлены лишние конструкции (`AS`, запятые, или другие элементы).

**Решение**:
1. Откройте метрику для редактирования
2. В поле **SQL expression** должно быть **ТОЛЬКО** выражение, например:
   - ✅ Правильно: `SUM(total_spend)`
   - ❌ Неправильно: `SUM(total_spend) AS total_spend`
   - ❌ Неправильно: `SUM(total_spend),`
   - ❌ Неправильно: `SELECT SUM(total_spend)`
3. Удалите все лишнее (AS, запятые, SELECT и т.д.)
4. Сохраните метрику
5. **Совет:** Используйте готовые выражения из `GOOGLE_SHEETS_METRICS_QUICK_REFERENCE.md`

### Проблема: Ошибка при выполнении запроса

**Решение**:
1. Проверьте SQL запрос в SQL Lab
2. Убедитесь, что все колонки существуют
3. Проверьте синтаксис Custom SQL метрик (см. решение выше)

## Структура данных в отчете

Основной запрос `google_sheets_campaigns_comparison.sql` группирует данные по кампаниям:
- **Одна запись на кампанию** для каждого типа атрибуции (1h и 24h)
- Все показатели суммируются за весь период
- Для фильтрации по дате используйте фильтр на Dashboard (колонки `min_date` и `max_date`)

### Группировка данных:

Запрос группирует по:
- `attribution_type` - тип атрибуции (1h или 24h) - для сравнения
- `campaign_name` - название кампании
- `campaignid` - ID кампании
- `format` - формат рекламы (оставлен для возможной фильтрации, но не влияет на группировку)

### Использование фильтра по дате:

1. На Dashboard добавьте фильтр **Date Range**
2. В настройках фильтра выберите колонку `min_date` или `max_date` (или обе)
3. Выберите нужный период
4. Фильтр автоматически отфильтрует данные в Chart

### Вариант: Полная группировка без разделения по атрибуции

Если вам нужна **одна запись на кампанию** без разделения на 1h и 24h, можно модифицировать SQL запрос:

1. Откройте Dataset в режиме редактирования
2. В SQL запросе уберите `attribution_type` из `SELECT` и `GROUP BY`
3. Объедините данные обоих типов атрибуции в одну запись

Но рекомендуется оставить разделение по `attribution_type` для сравнения эффективности разных типов атрибуции.

## Дополнительные советы

1. **Кэширование**: Superset кэширует результаты запросов. Если данные не обновляются, очистите кэш в настройках Dataset.

2. **Производительность**: Если запрос выполняется медленно, добавьте фильтры по дате в сам SQL запрос (опционально).

3. **Экспорт данных**: В Table Chart можно экспортировать данные в CSV, нажав на иконку экспорта.

4. **Автообновление**: В настройках Dashboard можно включить автообновление данных каждые N минут.

---

**Готово!** Теперь у вас есть полнофункциональный Dashboard для сравнения эффективности кампаний по типам атрибуции 1 HOUR и 24 HOURS.

