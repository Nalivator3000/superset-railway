# Настройка линейных графиков реактиваций (по дням, неделям и месяцам)

Эта инструкция поможет создать 3 графика реактиваций с разной группировкой:
- **По дням** - для детального анализа
- **По неделям** - для среднесрочных трендов
- **По месяцам** - для долгосрочных трендов

---

## Шаг 1: Создание Datasets

### 1.1. Dataset для графика по дням

1. **Откройте Superset** → **SQL Lab**
2. **Скопируйте SQL-запрос** из файла `reactivations_daily_trend.sql`
3. **Вставьте запрос** в SQL Lab
4. **Выполните запрос** (Run) - проверьте, что данные возвращаются
   - Должны быть колонки: `reactivation_date`, `inactivity_period`, `reactivations_count`
   - В колонке `inactivity_period` должны быть значения: `7-14 days`, `14-30 days`, `30-90 days`, `90+ days`
5. **Сохраните как Dataset:**
   - Нажмите **"Save"** → **"Save dataset"**
   - **Table name**: `reactivations_daily_trend`
   - **Schema**: `public` (или оставьте пустым)
   - Нажмите **"Save"**

### 1.2. Dataset для графика по неделям

1. **В SQL Lab** откройте новый запрос
2. **Скопируйте SQL-запрос** из файла `reactivations_weekly_trend.sql`
3. **Вставьте запрос** в SQL Lab
4. **Выполните запрос** (Run) - проверьте данные
   - Должны быть колонки: `week_start`, `inactivity_period`, `reactivations_count`
5. **Сохраните как Dataset:**
   - Нажмите **"Save"** → **"Save dataset"**
   - **Table name**: `reactivations_weekly_trend`
   - Нажмите **"Save"**

### 1.3. Dataset для графика по месяцам

1. **В SQL Lab** откройте новый запрос
2. **Скопируйте SQL-запрос** из файла `reactivations_monthly_trend.sql`
3. **Вставьте запрос** в SQL Lab
4. **Выполните запрос** (Run) - проверьте данные
   - Должны быть колонки: `month_start`, `inactivity_period`, `reactivations_count`
5. **Сохраните как Dataset:**
   - Нажмите **"Save"** → **"Save dataset"**
   - **Table name**: `reactivations_monthly_trend`
   - Нажмите **"Save"**

---

## Шаг 2: Создание Charts

### 2.1. Chart по дням

1. **Откройте Dataset** `reactivations_daily_trend` → нажмите **"Explore"**
2. **Выберите Chart Type**: **Line Chart**

**Настройка Query:**
- **Query Mode**: `Aggregate` ⚠️ **ВАЖНО!**

**Dimensions (Group by) - ОБЯЗАТЕЛЬНО добавить ОБА поля:**
1. В левой панели найдите раздел **Columns**
2. Найдите колонку `reactivation_date` (с иконкой часов 🕐)
3. **Перетащите** её в поле **Dimensions** справа (где "+ Drop columns here or click")
4. ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Найдите колонку `inactivity_period` (с иконкой "abc")
5. **Перетащите** её тоже в поле **Dimensions**
   - **БЕЗ этого поля все периоды смешаются в одну линию!**
   - **С этим полем каждая категория (7-14 days, 14-30 days, и т.д.) будет отдельной линией**

**Проверка Dimensions:**
- В поле **Dimensions** должно быть **2 элемента**:
  - `reactivation_date`
  - `inactivity_period` ⭐ (обязательно!)

**Metrics:**
1. В левой панели найдите раздел **Columns** (или **Metrics**)
2. Найдите колонку `reactivations_count` (с иконкой #)
3. **Перетащите** её в поле **Metrics** справа
4. Убедитесь, что тип метрики: **SUM** (суммируем количество)

**Настройка визуализации (вкладка Customize):**
- **Chart Type**: **Line Chart**
- **Time Column**: `reactivation_date`
- **X Axis**: `reactivation_date`
- **Y Axis**: `reactivations_count`
- **Series**: `inactivity_period` ⭐ (это создаст разные линии для каждого периода)
- **Show Legend**: включите (для отображения легенды с периодами)

**Проверка результата:**
- После нажатия **"Update chart"** должно быть **4 линии** на графике:
  - Одна для `7-14 days`
  - Одна для `14-30 days`
  - Одна для `30-90 days`
  - Одна для `90+ days`
- Если видите только **1 линию** - значит `inactivity_period` не добавлен в Dimensions!

3. **Сохраните Chart:**
   - **Chart name**: `Reactivations Daily Trend`
   - **Description**: `График реактиваций по дням с разбивкой по периодам неактивности`

### 2.2. Chart по неделям

1. **Откройте Dataset** `reactivations_weekly_trend` → нажмите **"Explore"**
2. **Выберите Chart Type**: **Line Chart**

**Настройка Query:**
- **Query Mode**: `Aggregate` ⚠️ **ВАЖНО!**

**Dimensions (Group by) - ОБЯЗАТЕЛЬНО добавить ОБА поля:**
1. Перетащите `week_start` в поле **Dimensions**
2. ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Перетащите `inactivity_period` в поле **Dimensions**
   - **БЕЗ этого поля все периоды смешаются в одну линию!**

**Проверка Dimensions:**
- В поле **Dimensions** должно быть **2 элемента**:
  - `week_start`
  - `inactivity_period` ⭐ (обязательно!)

**Metrics:**
- Перетащите `reactivations_count` в поле **Metrics**
- Тип: **SUM**

**Настройка визуализации (вкладка Customize):**
- **Chart Type**: **Line Chart**
- **Time Column**: `week_start`
- **X Axis**: `week_start`
- **Y Axis**: `reactivations_count`
- **Series**: `inactivity_period` ⭐
- **Show Legend**: включите

**Проверка результата:**
- Должно быть **4 линии** на графике (по одной для каждого периода)

3. **Сохраните Chart:**
   - **Chart name**: `Reactivations Weekly Trend`
   - **Description**: `График реактиваций по неделям с разбивкой по периодам неактивности`

### 2.3. Chart по месяцам

1. **Откройте Dataset** `reactivations_monthly_trend` → нажмите **"Explore"**
2. **Выберите Chart Type**: **Line Chart**

**Настройка Query:**
- **Query Mode**: `Aggregate` ⚠️ **ВАЖНО!**

**Dimensions (Group by) - ОБЯЗАТЕЛЬНО добавить ОБА поля:**
1. Перетащите `month_start` в поле **Dimensions**
2. ⚠️ **КРИТИЧЕСКИ ВАЖНО:** Перетащите `inactivity_period` в поле **Dimensions**
   - **БЕЗ этого поля все периоды смешаются в одну линию!**

**Проверка Dimensions:**
- В поле **Dimensions** должно быть **2 элемента**:
  - `month_start`
  - `inactivity_period` ⭐ (обязательно!)

**Metrics:**
- Перетащите `reactivations_count` в поле **Metrics**
- Тип: **SUM**

**Настройка визуализации (вкладка Customize):**
- **Chart Type**: **Line Chart**
- **Time Column**: `month_start`
- **X Axis**: `month_start`
- **Y Axis**: `reactivations_count`
- **Series**: `inactivity_period` ⭐
- **Show Legend**: включите

**Проверка результата:**
- Должно быть **4 линии** на графике (по одной для каждого периода)

3. **Сохраните Chart:**
   - **Chart name**: `Reactivations Monthly Trend`
   - **Description**: `График реактиваций по месяцам с разбивкой по периодам неактивности`

---

## Шаг 3: Создание Dashboard

1. **Dashboards** → **"+ Dashboard"**
2. **Dashboard name**: `Reactivations Analysis`
3. **Description**: `Анализ реактиваций по периодам неактивности (daily, weekly, monthly)`
4. Нажмите **"Save"**

---

## Шаг 4: Добавление Charts на Dashboard

1. **Откройте Dashboard** → **"Edit Dashboard"**
2. **Добавьте все 3 Chart:**
   - Нажмите **"+ Chart"**
   - Выберите `Reactivations Daily Trend`
   - Повторите для `Reactivations Weekly Trend` и `Reactivations Monthly Trend`
3. **Расположите Charts:**
   - Перетащите Charts в нужные места
   - Рекомендуется расположить их вертикально (один под другим)
   - Или в 3 колонки (если экран широкий)
4. **Настройте размеры** Charts (потяните за углы)
5. Нажмите **"Save"**

---

## Шаг 5: Добавление фильтров на Dashboard

### 5.1. Фильтр по дате для Daily Chart

1. Нажмите **"+ Filter"**
2. **Filter Type**: **Time Range**
3. **Dataset**: `reactivations_daily_trend`
4. **Column**: `reactivation_date`
5. **Filter name**: `Date Range (Daily)`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте `Reactivations Daily Trend`
7. Нажмите **"Save"**

### 5.2. Фильтр по дате для Weekly Chart

1. Нажмите **"+ Filter"** (создайте новый фильтр)
2. **Filter Type**: **Time Range**
3. **Dataset**: `reactivations_weekly_trend`
4. **Column**: `week_start`
5. **Filter name**: `Week Range (Weekly)`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте `Reactivations Weekly Trend`
7. Нажмите **"Save"**

### 5.3. Фильтр по дате для Monthly Chart

1. Нажмите **"+ Filter"** (создайте новый фильтр)
2. **Filter Type**: **Time Range**
3. **Dataset**: `reactivations_monthly_trend`
4. **Column**: `month_start`
5. **Filter name**: `Month Range (Monthly)`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте `Reactivations Monthly Trend`
7. Нажмите **"Save"**

### 5.4. Опционально: Фильтр по периоду неактивности

Если хотите фильтровать все 3 графика одновременно по периоду неактивности:

1. Нажмите **"+ Filter"**
2. **Filter Type**: **Select**
3. **Dataset**: `reactivations_daily_trend` (или любой из трех)
4. **Column**: `inactivity_period`
5. **Filter name**: `Inactivity Period`
6. **Scoping**: 
   - **Apply to specific charts**
   - Отметьте все 3 Chart
7. Нажмите **"Save"**

---

## Готово! ✅

Теперь у вас есть:
- ✅ 3 Chart с графиками реактиваций (daily, weekly, monthly)
- ✅ Несколько линий на каждом графике (по одной для каждого периода неактивности)
- ✅ Dashboard с фильтрами по дате для каждого графика
- ✅ Возможность выбирать период прямо на Dashboard

---

## Что показывает каждый график:

### Daily Trend (по дням):
- **X-axis**: Дата реактивации (каждый день)
- **Y-axis**: Количество реактиваций
- **Использование**: Детальный анализ, выявление дневных паттернов

### Weekly Trend (по неделям):
- **X-axis**: Начало недели (каждая неделя)
- **Y-axis**: Количество реактиваций
- **Использование**: Среднесрочные тренды, сглаживание дневных колебаний

### Monthly Trend (по месяцам):
- **X-axis**: Начало месяца (каждый месяц)
- **Y-axis**: Количество реактиваций
- **Использование**: Долгосрочные тренды, годовые сравнения

### Линии на каждом графике:
- `7-14 days` - реактивации через 1-2 недели
- `14-30 days` - реактивации через 2-4 недели
- `30-90 days` - реактивации через 1-3 месяца
- `90+ days` - реактивации через 3+ месяца

---

## Решение проблем

### Проблема: Все периоды смешались в одну линию

**Симптомы:**
- На графике видна только **1 линия** вместо 4
- Все периоды неактивности (7-14 days, 14-30 days, и т.д.) суммируются вместе

**Причина:**
- В поле **Dimensions** не добавлен `inactivity_period`
- Только `reactivation_date` (или `week_start`/`month_start`) добавлен в Dimensions

**Решение:**
1. Откройте Chart для редактирования
2. В разделе **Query** → **Dimensions** проверьте:
   - Должно быть **2 элемента**:
     - `reactivation_date` (или `week_start`/`month_start`)
     - `inactivity_period` ⭐
3. Если `inactivity_period` отсутствует:
   - В левой панели найдите колонку `inactivity_period`
   - Перетащите её в поле **Dimensions**
4. Перейдите на вкладку **Customize**
5. Убедитесь, что в поле **Series** выбрано `inactivity_period`
6. Нажмите **"Update chart"**
7. Теперь должно быть **4 линии** на графике

### Проблема: График не обновляется

**Симптомы:**
- Видно желтое предупреждение "Your chart is not up to date"
- Изменения в настройках не отражаются на графике

**Решение:**
1. Нажмите кнопку **"Update chart"** внизу панели Query
2. Или кликните на желтое предупреждение

### Проблема: Легенда не отображается

**Решение:**
1. Перейдите на вкладку **Customize**
2. Найдите опцию **"Show Legend"**
3. Включите её
4. Нажмите **"Update chart"**

## Дополнительные советы:

1. **Для сравнения с таблицей**: Добавьте на Dashboard Chart с типом **Table** на основе `reactivations_summary_by_period_simple.sql`
2. **Для синхронизации фильтров**: Используйте один фильтр по дате для всех 3 Chart (но это может быть сложнее в настройке)
3. **Для экспорта данных**: В каждом Chart можно экспортировать данные в CSV через иконку экспорта
